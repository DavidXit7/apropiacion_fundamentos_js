let longitud_lado = parseFloat(prompt("Ingrese la longitud del lado del cuadrado a calcular: "))

calcular_perimetro = 4 * longitud_lado

alert("El perimetro del cuadrado es de: " + calcular_perimetro)