let numero = parseInt(prompt("Ingrese un numero: "))

numero_duplicado = numero * numero;

numero_triplicado = numero * numero * numero;

alert("El doble del numero es de: "+ numero_duplicado)
alert("El triple del numero es de: " + numero_triplicado)