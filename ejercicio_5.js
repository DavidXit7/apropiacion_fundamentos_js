let temperatura = parseInt(prompt("Ingrese los grados celsius a convertir: ")); //Se solicita valor de grados celsius al usuario

conversion_fahrenheit = (temperatura * 9/5) + 32; //Se realiza la conversion a Fahrenheit

alert("La conversion a Fahrenheit es de: " +conversion_fahrenheit); //Se imprime el resultado de la conversion de grados